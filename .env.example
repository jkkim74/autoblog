# Copy to .env and fill in. The CLI loads .env automatically.

# Required for the Claude generator.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: only needed if you enable the webhook publisher.
AUTOBLOG_WEBHOOK_URL=
AUTOBLOG_WEBHOOK_TOKEN=
